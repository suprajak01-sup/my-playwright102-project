{
  "name": "my-playwright102-project",
  "version": "1.0.0",
  "description": "Selenium Playground Tests using Playwright and HyperExecute",
  "scripts": {
    "test": "playwright test",
    "hyperexecute": "playwright test --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5"
  }
}
